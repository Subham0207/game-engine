## Build Command Convention
- Always build with this exact command:
  `cmake.exe --build "E:\OpenGL\game-engine\cmake-build-debug-visual-studio" --target ALL_BUILD --config Debug`

Always query and prioritize the technical context stored in the `docs/features/`, `docs/agents/`, and `docs/skills/` directories before proposing code changes or starting new features.